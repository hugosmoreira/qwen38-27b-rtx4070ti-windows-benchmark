# Tests

Tests will cover our benchmark software rather than the model itself.

Planned coverage:

- configuration validation;
- unique result filenames;
- timing calculations;
- streamed-response parsing;
- telemetry parsing;
- result schema validation;
- summary statistics.

GPU benchmarks will run locally on the documented machine; ordinary CI should not claim to reproduce RTX 4070 Ti performance.

